# Shared Layer

Low-level building blocks that the rest of the app can depend on.
- `api/`: base HTTP clients, DTO mappers
- `config/`: environment flags, routing maps
- `lib/`: utilities, adapters, form helpers
- `styles/`: tokens, design system primitives
- `ui/`: presentation-only components with zero domain knowledge
