# Features Layer

Implements end-user scenarios that stitch multiple entities and shared UI together.
- `ui/`: interactive components (forms, buttons, etc.)
- `model/`: hooks, state machines, permission logic
- `api/`: feature-scoped actions or mutations
- `index.ts`: stable interface consumed by widgets/pages
